# Declarando-a-fun-o-Estrutura-de-Repeti-o-While-
#include <stdio.h>

int main()

{
    //Declarando a função Estrutura de Repetição While 
   int x = 0;
    while(x <= 20) {
        printf ("%d\n",x);
        x++;
        
    }

    return 0;
}
